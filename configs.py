import sys

token = 'TOKEN'
id = "ID"
prefix = ">"
logger = False

if(token == ""):
    print("Please enter a discord token")
    sys.exit(0)

if(id == ""):
    print("Please enter a user ID corresponding to the token")
    sys.exit(0)
