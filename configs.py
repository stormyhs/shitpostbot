import sys

token = "TOKEN"
id = "USERID"
prefix = ";"

if(token == ""):
    print("please enter a discord Token ")
    sys.exit(0)

if(id == ""):
    print("Please enter a local id")
    sys.exit(0)
